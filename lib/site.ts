/**
 * Public identity of the landing site — the one source for metadata,
 * robots.txt, sitemap.xml, the web manifest and structured data.
 *
 * `siteUrl` is the canonical origin. www.bilig.systems serves the same
 * pages without redirecting, so every canonical link points here to keep
 * search engines from indexing the two hosts as duplicates.
 */
export const siteUrl = "https://bilig.systems";

export const siteName = "Bilig";

export const siteTitle = "Bilig | Сургалтын төвийн удирдлагын систем";

export const siteDescription =
  "Хуваарь, ирц, төлбөр, эцэг эхийн харилцаа. Сургалтын төвийн бүх ажил нэг системд. 14 хоног үнэгүй туршаарай.";
